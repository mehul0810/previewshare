{
	"$schema": "https://playground.wordpress.net/blueprint-schema.json",
	"landingPage": "/wp-admin/options-general.php?page=previewshare_settings",
	"preferredVersions": {
		"php": "8.0",
		"wp": "latest"
	},
	"phpExtensionBundles": [
		"kitchen-sink"
	],
	"features": {
		"networking": true
	},
	"steps": [
		{
			"step": "login",
			"username": "admin",
			"password": "password"
		},
		{
			"step": "installPlugin",
			"pluginZipFile": {
				"resource": "wordpress.org/plugins",
				"slug": "previewshare"
			},
			"options": {
				"activate": true
			}
		},
		{
			"step": "setSiteOptions",
			"options": {
				"blogname": "PreviewShare Demo",
				"permalink_structure": "/%postname%/"
			}
		},
		{
			"step": "runPHP",
			"code": "<?php require_once 'wordpress/wp-load.php'; if ( function_exists( 'previewshare_update_settings' ) ) { previewshare_update_settings( array( 'default_ttl_hours' => 48, 'enable_caching' => true, 'post_types' => array( 'post', 'page' ) ) ); } $existing = get_page_by_path( 'previewshare-demo-draft', OBJECT, 'post' ); if ( ! $existing ) { wp_insert_post( array( 'post_title' => 'PreviewShare demo draft', 'post_name' => 'previewshare-demo-draft', 'post_status' => 'draft', 'post_type' => 'post', 'post_content' => '<!-- wp:paragraph --><p>This draft is ready to share with PreviewShare. Open the editor to generate a secure preview link.</p><!-- /wp:paragraph -->' ) ); }"
		}
	]
}
